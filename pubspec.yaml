name: api_sample
description: Retrofit API sample
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # add this
  provider: ^5.0.0
  retrofit: ^2.0.0
  freezed: ^0.14.2
  freezed_annotation: ^0.14.2
  webview_flutter: ^2.0.8
dev_dependencies:
  # add this
  build_runner: any
  retrofit_generator: any
  json_serializable:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true